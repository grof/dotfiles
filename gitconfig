[user]
  name = Francois Groleau
  email = francois.groleau@gmail.com
[color]
  ui = auto
[alias]
  st      = status --short --branch
  b       = branch
  ba      = branch -a
  bav     = branch -a -vvv
  cb      = checkout -b
  cm      = checkout master
  co      = checkout
  out     = log FETCH_HEAD..HEAD
  in      = log HEAD..FETCH_HEAD
  ignored = !git clean -ndX | sed -e 's/^Would remove //'
  ffm     = merge --ff-only
  ffp     = pull --ff-only
  cp      = cherry-pick
[diff]
  noprefix = true
  tool = vimdiff
[push]
  default = current
[core]
  excludesfile = ~/.gitignore
